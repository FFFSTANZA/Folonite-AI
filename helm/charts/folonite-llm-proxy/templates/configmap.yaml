apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "folonite-llm-proxy.fullname" . }}-config
  labels:
    {{- include "folonite-llm-proxy.labels" . | nindent 4 }}
data:
  litellm-config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}