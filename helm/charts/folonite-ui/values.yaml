replicaCount: 1

image:
  repository: ghcr.io/folonite-ai/folonite-ui
  tag: edge
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 9992
  targetPort: 9992
  annotations: {}

resources:
  limits:
    memory: "1Gi"
    cpu: "500m"
  requests:
    memory: "512Mi"
    cpu: "250m"

nodeSelector: {}

tolerations: []

affinity: {}

env:
  NODE_ENV: "production"
  FOLONITE_AGENT_BASE_URL: "http://folonite-agent:9991"
  FOLONITE_DESKTOP_VNC_URL: "http://folonite-desktop:9990/websockify"

config:
  agentBaseUrl: "http://folonite-agent:9991"
  desktopVncUrl: "http://folonite-desktop:9990/websockify"

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: folonite.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80